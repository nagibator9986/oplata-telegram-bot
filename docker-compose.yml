services:
  tenribot:
    build: .
    env_file: .env
    volumes:
      - tenribot_data:/app/data
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  tenribot_data:
