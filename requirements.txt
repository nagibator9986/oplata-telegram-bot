aiogram>=3.13,<4
SQLAlchemy>=2.0.30
# greenlet — обязателен для async-движка SQLAlchemy (create_async_engine). На части платформ
# (в т.ч. Python 3.13) он НЕ ставится транзитивно, и init_db() падает с
# "the greenlet library is required" — фиксируем явно, чтобы деплой не ломался.
greenlet>=3.1
aiosqlite>=0.20
pydantic-settings>=2.4
httpx>=0.27
openai>=1.40
